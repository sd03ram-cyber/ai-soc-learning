# Day 2 – Reflection

**3 things learned:**
1. Linux file system layout (/etc, /var, /home, /tmp) and what each directory is for.
2. File permission model (rwx, chmod, chown) and how it maps to users/groups/others.
3. How to tail live logs (`tail -f`, `journalctl`) — same idea a SOC analyst uses to watch activity in real time.

**1 thing confusing:**
- Exact numeric permission values (e.g. 644 vs 755) and how they map to rwx combinations for owner/group/other — need more practice converting between symbolic and numeric.
